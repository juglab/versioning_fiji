package sc.fiji.versioning.command.session;

import org.scijava.command.Command;
import org.scijava.plugin.Plugin;

@Plugin(type= Command.class, label = "Help>Sessions>Add session..")
public class AddSessionCommand implements Command {
	@Override
	public void run() {
		//TODO
	}
}
